meta {
  name: get collection by id (not found)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/collection/{{missingId}}
  body: none
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

assert {
  res.status: eq 404
  res.body.status: eq 404
  res.body.data: isNull
}
