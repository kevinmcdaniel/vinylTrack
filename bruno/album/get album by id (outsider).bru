meta {
  name: get album by id (outsider)
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/api/album/{{albumId}}
  body: none
  auth: inherit
}

headers {
  x-user-email: {{outsiderEmail}}
}

assert {
  res.status: eq 404
  res.body.status: eq 404
  res.body.data: isNull
}
