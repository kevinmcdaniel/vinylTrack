meta {
  name: delete album
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/album/{{albumId}}
  body: none
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

assert {
  res.status: eq 200
  res.body.status: eq 200
}
