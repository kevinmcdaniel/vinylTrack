meta {
  name: delete found copy
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/copy/{{foundCopyId}}
  body: none
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

assert {
  res.status: eq 200
  res.body.status: eq 200
}
