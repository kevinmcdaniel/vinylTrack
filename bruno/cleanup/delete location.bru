meta {
  name: delete location
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/location/{{locationId}}
  body: none
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

assert {
  res.status: eq 200
  res.body.status: eq 200
}
