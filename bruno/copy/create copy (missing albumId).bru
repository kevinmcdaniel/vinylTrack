meta {
  name: create copy (missing albumId)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/copy
  body: json
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

body:json {
  {
    "locationId": "{{childLocationId}}",
    "ownerId": "{{kevinOwnerId}}"
  }
}

assert {
  res.status: eq 406
  res.body.status: eq 406
}
