meta {
  name: create owner (missing name)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/owner
  body: json
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

body:json {
  {
    "userId": "{{kevinUserId}}"
  }
}

assert {
  res.status: eq 406
  res.body.status: eq 406
}
