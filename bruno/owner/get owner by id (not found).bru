meta {
  name: get owner by id (not found)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/owner/{{missingId}}
  body: none
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

assert {
  res.status: eq 404
  res.body.status: eq 404
  res.body.data: isNull
}
