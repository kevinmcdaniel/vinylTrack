meta {
  name: get artist by id (not found)
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/artist/{{missingId}}
  body: none
  auth: inherit
}

headers {
  x-user-email: {{ownerEmail}}
}

assert {
  res.status: eq 404
  res.body.status: eq 404
  res.body.data: isNull
}
