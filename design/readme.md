# vinylTrack — Design

`system/` was copied over from squaretrack's design system (tokens, foundation guidelines, component mockups) as a starting point. It's squaretrack's look-and-feel as-is — **not yet reviewed or adapted for vinylTrack** (naming, colors, branding all still say squaretrack in places). Not wired into the FE app yet either.

See the tracked issue for adapting this to vinylTrack before it's actually used.
