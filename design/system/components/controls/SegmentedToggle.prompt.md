Compact mono segmented control for 2–3 mutually-exclusive modes.

```jsx
<SegmentedToggle options=["Scripted", "Sight"] value={mode} onChange={setMode} />
```

The active segment fills with the accent-soft tint. Use for the Scripted/Sight calling switch and similar small mode picks.
